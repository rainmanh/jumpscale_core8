# Discover

```shell
Discover AYS repository on the filesystem and save them in DB.

Options:
  -p, --path TEXT  Path where to start looking for AYS repository
  --help           Show this message and exit.
```
