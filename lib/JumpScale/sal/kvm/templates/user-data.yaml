#cloud-config
chpasswd:
  expire: false
ssh_pwauth: true
users:
  - lock-passwd: false
    name: cloudscalers
    plain_text_passwd: gig1234
    shell: /bin/bash
    ssh-authorized-keys:
      - {{pubkey}}
    sudo: ALL=(ALL) ALL
